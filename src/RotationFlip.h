#ifndef __ROTATION_FLIP_H__
#define __ROTATION_FLIP_H__

enum FlipType {
  NONE,
  FLIP_HORIZONTAL,
  FLIP_VERTICAL,
  FLIP_BOTH
};

enum RotationType {
  ROTATE_0,
  ROTATE_90,
  ROTATE_180,
  ROTATE_270
};

#endif
